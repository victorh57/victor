hrs=input("Enter hours")
h=float(hrs)
rate=input("Enter rate per hour")
r=float(rate)
def computepay(h,r):
    if(h<40):
        return print(h*r)
    else:
        truework=(h-40)*(r*1.5)+40*r
        return print(truework)
computepay(h,r)


